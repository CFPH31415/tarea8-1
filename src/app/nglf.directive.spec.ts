import { NglfDirective } from './nglf.directive';

describe('NglfDirective', () => {
  it('should create an instance', () => {
    const directive = new NglfDirective();
    expect(directive).toBeTruthy();
  });
});
